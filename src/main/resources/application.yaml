server:
  servlet:
    context-path: /moviebook
  port: 8081
  error:
    include-message: always

spring:
  data:
    mongodb:
      host: moviebook-database-mongo-container
      uri: mongodb://localhost/moviebook-database-mongo-container
  profiles:
    include: 'integration'